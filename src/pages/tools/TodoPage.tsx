import { TodoManager } from '@/components/tools/TodoManager';

export default function TodoPage() {
  return (
    <div className="container mx-auto px-4 py-8 max-w-7xl">
      <TodoManager />
    </div>
  );
}